name: bar
description: Description of bar. bar-desc.
license: MIT
dependencies:
  foo:
    url: localhost:<[*PORT*]>/pkg/foo
    version: 1.2.3
  sub:
    url: localhost:<[*PORT*]>/pkg/sub
    version: ^3.0.0
environment:
  sdk: ^0.1.20
