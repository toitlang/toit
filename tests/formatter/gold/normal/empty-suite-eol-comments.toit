main value:
  if value == 0: return
  else if value == 1: // Intentionally empty.
  else if value == 2: // Also intentionally empty.
  else if value == 3:
    // This comment is the empty suite's body.
  else: print value
